---
---
// Файл с конфигурацией сайта, который обрабатывается Jekyll
window.siteConfig = {
  "baseUrl": "{{ site.baseurl }}",
  "formSubmissionEmail": "{{ site.form_submission_email }}",
  "telegramBotToken": "{{ site.telegram_bot_token }}",
  "telegramChatId": "{{ site.telegram_chat_id }}"
};
